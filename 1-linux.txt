sudo apt install ./deb.file
sudo ip a --------------------------internal ipsudo 
hostname -I -------------------hostname
sudo curl ifconfig.me --------------external ip
sudo apt install net-tools
ifconfig
sudo mkdir foldername
sudo touch filename
sudo mv oldfoldrname newfoldername //rename
sudo mv nginx //test move file to folder
sudo rm -r * //remove all
sudo cp original_name copyed_name
sudo chmod ugo+rwx filename //give permission to file read and write
Below gives all permissions to everybody to all files and subdirectories:
chmod -R a+rwx path

pass -------------
1 type passwd on putty comandline
2 paste new pass


change timezone
timedatectl
timedatectl set-timezone Europe/Istanbul

free -m
nproc
